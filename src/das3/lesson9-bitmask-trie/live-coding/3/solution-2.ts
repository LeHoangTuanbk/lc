class TrieNode {
  child: (TrieNode | null)[] = [null, null];
  index: number = -1;
}

function add(ai: number, id: number) {}

function query(ai: number) {}

function solve(n: number, k: number) {
  const root = new TrieNode();
  let besti = -1,
    bestj = -1,
    bestx = -1,
    bestVal = -1;
}

export function lisaMartians(a: number[], k: number) {}
