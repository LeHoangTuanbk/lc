function numSubseq(nums: number[], target: number): number {
  const n = nums.length;
  for (let i = 0; i < n; i++) {
    for (let j = i + 1; j < n; j++) {
      const tempNums = nums.slice(i, j + 1);
      const min = 
    }
  }
}

